// Khởi tạo dự án
B1: npm init 
B2: npm install express - khởi tạo thư viên express

// Cài đặt nodemon : npm i nodemon --save-dev 

// Cài thư viện Morgan để logger request được gửi lên node sever  npm i morgan --save-dev

// Thư viện (template engines) handlebars : npm i handlebars --save-dev

// npm run watch khởi chạy scss


// Tải trang lần đầu trình duyệt sẽ trả về code 200, nếu ko có gì thay đổi code 300 not modified

// JS lên dùng nháy đơn để xử lý chuỗi

// Mongoose thư viện giúp liên kết chặt trẽ giữa JS kết nối với MogoDB

// Nếu gán biến lại thì khai báo là: let, const ko gán được biến